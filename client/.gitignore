# Playwright E2E (FE7)
e2e/.tmp/
test-results/
playwright-report/
playwright/.cache/

# vite-plugin-pwa dev output (devOptions.enabled)
dev-dist/

# The native apps' address screen (npm run build:native), copied into
# android/ and ios/ by `cap sync`
native-dist/
