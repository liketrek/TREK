# Journey export designer

This boundary will own template and layout editing. It will work from the
`model/` contract and produce render options, without reaching into the
Journey store or preview DOM directly.

The current Journey Book keeps its existing fixed layout; a designer UI is a
separate follow-up.
